/**
 * Coins to open a paid pack: a global pack, or a repeat Trip Pack in a city the
 * user has already claimed their one free pack in. Client-safe (no server-only
 * imports) so both API logic and UI can reference it.
 */
export const PACK_COST = 250;
